# ==========================================================================
# Cybergy Talent — Backend Python dependencies
# Pinned to major.minor for reproducible, secure builds.
# ==========================================================================

# --- Web framework (async) ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.32          # parses multipart/form-data (file uploads)

# --- Data validation ---
pydantic==2.7.4
pydantic-settings==2.3.4         # loads settings from environment variables

# --- Database (async SQLAlchemy + PostgreSQL + migrations) ---
sqlalchemy[asyncio]==2.0.31
asyncpg==0.29.0                  # async PostgreSQL driver
alembic==1.13.2                  # database migrations
greenlet==3.0.3                  # required by SQLAlchemy async

# --- Resume file parsing ---
PyMuPDF==1.24.5                  # "fitz" — fast PDF text extraction
pdfplumber==0.11.1               # layout-aware PDF fallback
python-docx==1.1.2               # Word .docx parsing

# --- AI mapping (OpenAI-compatible client for Abacus AI) ---
openai==1.35.7

# --- XML generation ---
lxml==5.2.2

# --- Security ---
slowapi==0.1.9                   # rate limiting for FastAPI
python-magic==0.4.27             # real MIME sniffing (defense in depth)

# --- Testing / quality (also used by CI) ---
pytest==8.2.2
pytest-asyncio==0.23.7
httpx==0.27.0                    # async test client
